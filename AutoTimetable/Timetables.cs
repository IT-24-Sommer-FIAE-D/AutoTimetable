namespace AutoTimetable
{
    public class Timetables
    {
        private string url;
        private string className;

        public Timetables(string url, string className)
        {
            this.url = url;
            this.className = className;
        }


    }
}